targets:
  ImageService:
    type: library.static
    platform: iOS
    sources:
      - path: Sources
        name: ImageService
    settings:
      INFOPLIST_FILE: ../ModuleInfo.plist
    configFiles:
      Debug: ../Configs/Module.xcconfig
      Release: ../Configs/Module.xcconfig
